# robots.txt - nashos.ai
# All search engines and AI crawlers are welcome.
#
# Non-public routes are disallowed; everything else is open, including /try and
# /demo, which are commercially important and listed in the sitemap.
#
# IMPORTANT: a crawler obeys ONLY its most specific matching group and ignores
# `*` entirely. Every Disallow under `*` is therefore REPEATED verbatim in each
# named group below - otherwise the named bots would silently ignore it. If you
# add a Disallow, add it to every group.
#
# /sign-in is deliberately NOT disallowed: it is served with `noindex`, and a
# blocked page is one Google cannot crawl to discover the noindex on.


# Default: allow everyone
User-agent: *
Allow: /
Disallow: /api/

# --- Google ---
User-agent: Googlebot
Allow: /
Disallow: /api/

User-agent: Googlebot-Image
Allow: /
Disallow: /api/

User-agent: Googlebot-News
Allow: /
Disallow: /api/

User-agent: Google-Extended
Allow: /
Disallow: /api/

User-agent: GoogleOther
Allow: /
Disallow: /api/

# --- Bing / Copilot ---
User-agent: Bingbot
Allow: /
Disallow: /api/

# --- DuckDuckGo ---
User-agent: DuckDuckBot
Allow: /
Disallow: /api/

User-agent: DuckAssistBot
Allow: /
Disallow: /api/

# --- OpenAI ---
User-agent: GPTBot
Allow: /
Disallow: /api/

User-agent: OAI-SearchBot
Allow: /
Disallow: /api/

User-agent: ChatGPT-User
Allow: /
Disallow: /api/

# --- Anthropic ---
User-agent: ClaudeBot
Allow: /
Disallow: /api/

User-agent: Claude-SearchBot
Allow: /
Disallow: /api/

User-agent: Claude-User
Allow: /
Disallow: /api/

# --- Perplexity ---
User-agent: PerplexityBot
Allow: /
Disallow: /api/

User-agent: Perplexity-User
Allow: /
Disallow: /api/

# --- Common Crawl ---
User-agent: CCBot
Allow: /
Disallow: /api/

# --- Meta AI ---
User-agent: FacebookBot
Allow: /
Disallow: /api/

User-agent: meta-externalagent
Allow: /
Disallow: /api/

# --- Amazon ---
User-agent: Amazonbot
Allow: /
Disallow: /api/

# --- ByteDance ---
User-agent: Bytespider
Allow: /
Disallow: /api/

# --- Apple ---
User-agent: Applebot
Allow: /
Disallow: /api/

User-agent: Applebot-Extended
Allow: /
Disallow: /api/

# --- Cohere ---
User-agent: cohere-ai
Allow: /
Disallow: /api/

# --- You.com ---
User-agent: YouBot
Allow: /
Disallow: /api/

# Retired user-agent strings, kept so older crawler builds still match a group.
User-agent: anthropic-ai
Allow: /
Disallow: /api/

User-agent: Claude-Web
Allow: /
Disallow: /api/

# ------------------------------------------------------------------
# Sitemap
# ------------------------------------------------------------------
# Only the machine-readable XML sitemap is declared here. /sitemap is the
# human-readable HTML sitemap page (a normal indexable page, not a sitemap
# file) - declaring it as a Sitemap: target is invalid per sitemaps.org.

Sitemap: https://nashos.ai/sitemap.xml

# llms.txt / llms-full.txt provide a curated, structured summary of this site's
# content specifically for LLMs and AI answer engines (https://llmstxt.org).
# Every capability claim in them is checked against the codebase before it is
# published; time-saving figures are labelled as design targets, not measured
# customer results. Keep them that way - these files get quoted verbatim.
# llms.txt:      https://nashos.ai/llms.txt
# llms-full.txt: https://nashos.ai/llms-full.txt
